
let TODOAction = {
    toggleItemList = (ID) => { // 不需要传todos的原因在于：store中存了需要改变的todos

    },
    deleteItem = (ID) => {

    },
    createItem = (title) => {

    },
    editItem = (id, title) => {

    },
    loadData = () => {

    }
}